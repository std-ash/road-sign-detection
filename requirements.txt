# Make sure numpy is installed first with a specific version
numpy==1.22.4

# Core dependencies
opencv-python-headless==4.5.5.64
pillow==9.0.1

# CPU-only versions of PyTorch - specific versions to ensure compatibility
--find-links https://download.pytorch.org/whl/torch_stable.html
torch==1.9.0+cpu
torchvision==0.10.0+cpu

# Minimal dependencies for YOLOv5
pandas==1.3.5
tqdm==4.64.0

# Web server
flask==2.0.3
flask-cors==3.0.10
gunicorn==20.1.0

# Utilities
requests==2.27.1
