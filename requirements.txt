# Core dependencies - minimal set for Heroku
numpy==1.22.4
pillow==9.1.1

# Web server with fixed versions to avoid compatibility issues
flask==2.0.1
werkzeug==2.0.2
flask-cors==3.0.10
gunicorn==20.1.0
jinja2==3.0.3
itsdangerous==2.0.1
click==8.0.3

# Utilities - only what's needed
requests==2.27.1
